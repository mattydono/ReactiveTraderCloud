import { AdaptiveLoader } from 'rt-components';
import React from 'react';

export const ResponseLoader: React.FC = () => (
  <AdaptiveLoader size={24} speed={0.8} seperation={1.5} type="secondary"/>
)
